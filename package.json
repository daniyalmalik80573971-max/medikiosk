{
  "name": "medikiosk",
  "version": "1.0.0",
  "description": "MediKiosk - AI Powered Patient Case-Taking Software (SIH26047)",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
